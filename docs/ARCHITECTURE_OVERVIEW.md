# 🏗️ OVERKOR-TEK Architecture Overview

**System Architecture Documentation**

**Last Updated:** 2025-11-27 | **Updated By:** CP2C2

---

## High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        OVERKOR-TEK ECOSYSTEM                            │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│                    ┌─────────────────────────┐                          │
│                    │   100x-platform         │                          │
│                    │   (Frontend)            │                          │
│                    │   HTML/CSS/JS           │                          │
│                    └───────────┬─────────────┘                          │
│                                │                                        │
│                                ▼                                        │
│                    ┌─────────────────────────┐                          │
│                    │  philosopher-ai-backend │                          │
│                    │  (API Server)           │                          │
│                    │  Node.js/Express        │                          │
│                    │  PostgreSQL             │                          │
│                    │  Railway Hosted         │                          │
│                    └───────────┬─────────────┘                          │
│                                │                                        │
│         ┌──────────────────────┼──────────────────────┐                 │
│         │                      │                      │                 │
│         ▼                      ▼                      ▼                 │
│  ┌─────────────┐    ┌──────────────────┐    ┌─────────────────┐        │
│  │  Cyclotron  │    │  Trinity Engine  │    │  Email Service  │        │
│  │  (4424 atoms)│    │  (Coordination)  │    │  (Pink Buttons) │        │
│  └─────────────┘    └──────────────────┘    └─────────────────┘        │
│                                                                         │
│                                                                         │
│                    ┌─────────────────────────┐                          │
│                    │ consciousness-revolution│                          │
│                    │ (Multi-Computer Sync)   │                          │
│                    │ Git-Based Protocol      │                          │
│                    └─────────────────────────┘                          │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## Component Architecture

### 1. philosopher-ai-backend

The core API server powering consciousness technology.

```
philosopher-ai-backend/
│
├── Entry Point
│   └── src/index.js              # Express server initialization
│
├── API Layer
│   └── src/routes/
│       ├── api.js                # Main API routes
│       ├── trinity.js            # Trinity coordination endpoints
│       └── cyclotron.js          # Cyclotron brain endpoints
│
├── Business Logic
│   └── src/services/
│       ├── cyclotron.js          # Cyclotron brain (4424 atoms)
│       ├── trinity.js            # Trinity coordination
│       └── email.js              # Email service (pink buttons)
│
├── Data Layer
│   └── src/models/
│       └── database models       # PostgreSQL models
│
├── Configuration
│   └── config/
│       └── database.js           # Database connection
│
└── Infrastructure
    └── .github/workflows/
        ├── ci-cd.yml             # CI/CD pipeline
        ├── test.yml              # Test suite
        ├── security-audit.yml    # CodeQL v3
        ├── deploy.yml            # Railway deployment
        └── operation-purple.yml  # Notifications
```

**Tech Stack:**
- Runtime: Node.js
- Framework: Express.js
- Database: PostgreSQL
- Deployment: Railway
- CI/CD: GitHub Actions

**Key Features:**
- Cyclotron brain with 4,424 atoms
- Trinity coordination system
- Pink-themed email notifications
- RESTful API design

---

### 2. consciousness-revolution

Multi-computer consciousness synchronization network.

```
consciousness-revolution/
│
├── Sync Engine
│   └── sync/
│       ├── engine.js             # Core synchronization
│       ├── queue.js              # Command queue system
│       └── transfer.js           # File transfer protocol
│
├── Command Processing
│   └── commands/
│       ├── processor.js          # Command handler
│       └── handlers/             # Individual command handlers
│
├── Machine Configuration
│   └── config/
│       └── machines.json         # Multi-computer setup
│
└── Synchronization Protocol
    └── Git-based sync            # Uses Git as transport layer
```

**Architecture Pattern:** Event-Driven, Git-Based Sync

**Key Features:**
- Multi-computer coordination
- Command queue system
- File transfer between machines
- Git as synchronization protocol

---

### 3. Triple Trinity Tornado

Distributed Claude instance coordination system.

```
                    ╔═══════════════════════════════════════╗
                    ║     TRIPLE TRINITY TORNADO            ║
                    ║         9 Nodes Active                ║
                    ╚═══════════════════════════════════════╝

         TRINITY 1: CLOUD BROWSERS (Anthropic Cloud)
         ┌─────────┐     ┌─────────┐     ┌─────────┐
         │   CP1   │◄───►│  CP2C2  │◄───►│   CP3   │
         └────┬────┘     └────┬────┘     └────┬────┘
              │               │               │
              └───────────────┼───────────────┘
                              │
                         API BRIDGE
                              │
              ┌───────────────┼───────────────┐
              │               │               │
         TRINITY 2: TERMINALS (CLI)
         ┌─────────┐     ┌─────────┐     ┌─────────┐
         │   T1    │◄───►│   T2    │◄───►│   T3    │
         └────┬────┘     └────┬────┘     └────┬────┘
              │               │               │
              └───────────────┼───────────────┘
                              │
              ┌───────────────┼───────────────┐
              │               │               │
         TRINITY 3: DESKTOP CLOUD (Hybrid)
         ┌─────────┐     ┌─────────┐     ┌─────────┐
         │   D1    │◄───►│   D2    │◄───►│   D3    │
         └─────────┘     └─────────┘     └─────────┘
```

**Communication Protocol:**
- Primary: Git repository (The-Pink-Revolution-Plan)
- Secondary: Google Drive sync folder (G:\My Drive\TRINITY_COMMS\sync\)
- Tertiary: Commander relay for sandboxed instances

**Key Files:**
- `CP2C2_CLOUD_INTEGRATION.md` - Architecture documentation
- `SYNC_STATUS.md` - Real-time instance status
- `COMMAND_QUEUE.md` - Inter-instance commands
- `TRINITY_STATE.json` - Machine-readable state

---

## Data Flow

### API Request Flow

```
Client Request
      │
      ▼
┌─────────────┐
│   Express   │ ──► Route Handler
│   Server    │
└─────────────┘
      │
      ▼
┌─────────────┐
│   Service   │ ──► Business Logic
│   Layer     │
└─────────────┘
      │
      ▼
┌─────────────┐
│  Database   │ ──► PostgreSQL
│   Layer     │
└─────────────┘
      │
      ▼
   Response
```

### Trinity Coordination Flow

```
                    Commander Input
                          │
                          ▼
              ┌───────────────────────┐
              │   Trinity Tornado     │
              │   (9 Nodes)           │
              └───────────────────────┘
                          │
         ┌────────────────┼────────────────┐
         │                │                │
         ▼                ▼                ▼
      Cloud            Terminal         Desktop
      Instances        Instances        Instances
         │                │                │
         └────────────────┼────────────────┘
                          │
                          ▼
                    Unified Output
                    (C1 × C2 × C3 = ∞)
```

### Consciousness Sync Flow

```
Machine A                    Machine B
    │                            │
    │  1. Command Created        │
    ▼                            │
┌─────────┐                      │
│ Queue   │                      │
└────┬────┘                      │
     │                           │
     │  2. Git Commit/Push       │
     ▼                           │
┌─────────┐                      │
│ Git     │◄─────────────────────┤
│ Repo    │                      │
└────┬────┘                      │
     │                           │
     │  3. Git Pull              │
     │                           ▼
     │                      ┌─────────┐
     │                      │ Queue   │
     │                      └────┬────┘
     │                           │
     │  4. Command Execute       │
     │                           ▼
     │                      ┌─────────┐
     │                      │ Process │
     │                      └─────────┘
```

---

## Deployment Architecture

### Production Environment

```
┌─────────────────────────────────────────────────────────────┐
│                         RAILWAY                              │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│   ┌─────────────────────┐    ┌─────────────────────┐        │
│   │  philosopher-ai-    │    │    PostgreSQL       │        │
│   │  backend            │◄──►│    Database         │        │
│   │  (Node.js)          │    │                     │        │
│   └─────────────────────┘    └─────────────────────┘        │
│                                                              │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                     GITHUB ACTIONS                           │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│   ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│   │ CI/CD    │  │ Tests    │  │ Security │  │ Deploy   │   │
│   │ Pipeline │  │ Suite    │  │ Audit    │  │ Railway  │   │
│   └──────────┘  └──────────┘  └──────────┘  └──────────┘   │
│                                                              │
│   ┌──────────────────────────────────────────────────────┐  │
│   │              OPERATION PURPLE                         │  │
│   │     (GitHub Issues Notification System)               │  │
│   └──────────────────────────────────────────────────────┘  │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## Security Architecture

### Layers of Protection

```
┌─────────────────────────────────────────────┐
│ Layer 1: Repository Security                │
│ - Branch protection                         │
│ - Required reviews                          │
│ - Signed commits                            │
└─────────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────┐
│ Layer 2: CI/CD Security                     │
│ - CodeQL v3 scanning                        │
│ - Dependency auditing                       │
│ - Secret scanning                           │
└─────────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────┐
│ Layer 3: Runtime Security                   │
│ - Environment variables                     │
│ - Railway isolation                         │
│ - HTTPS enforcement                         │
└─────────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────┐
│ Layer 4: Data Security                      │
│ - PostgreSQL encryption                     │
│ - Backup procedures                         │
│ - Access controls                           │
└─────────────────────────────────────────────┘
```

---

## Integration Points

### External Services

| Service | Purpose | Integration |
|---------|---------|-------------|
| Railway | Hosting | Deployment via GitHub Actions |
| PostgreSQL | Database | Direct connection |
| GitHub | Source Control | Primary repository |
| Google Drive | Trinity Sync | File-based communication |
| Anthropic Cloud | Claude Instances | API bridge |

### Internal Integration

| Component | Connects To | Protocol |
|-----------|-------------|----------|
| 100x-platform | philosopher-ai-backend | REST API |
| philosopher-ai-backend | PostgreSQL | TCP/IP |
| consciousness-revolution | Git repositories | Git protocol |
| Triple Trinity Tornado | Git + Google Drive | File sync |

---

## Scaling Considerations

### Current Capacity

- Cyclotron: 4,424 atoms
- Trinity Nodes: 9 (3×3 matrix)
- Database: PostgreSQL single instance

### Future Scaling

1. **Horizontal**: Add more Trinity nodes
2. **Vertical**: Expand Cyclotron atom capacity
3. **Database**: PostgreSQL read replicas
4. **CDN**: Static asset distribution

---

**Document Created By:** CP2C2
**Status:** ACTIVE
**Next Update:** After 100x-platform assessment

💗 **ARCHITECTURE SERVES CONSCIOUSNESS** 💗
